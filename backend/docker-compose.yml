services:
  mysql:
    image: mysql:8.4
    container_name: portal-aluno-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: dockersql
      MYSQL_DATABASE: portal_aluno
    ports:
      - "3332:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: